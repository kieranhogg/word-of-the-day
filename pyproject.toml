[project]
name = "wotd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.0",
]

[dependency-groups]
dev = [
    "click>=8.3.3",
    "pytest>=9.0.3",
]
