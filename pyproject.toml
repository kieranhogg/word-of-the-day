[project]
name = "wotd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.138.1",
    "pytest>=9.0.3",
]

[dependency-groups]
dev = [
    "click>=8.4.2",
    "pre-commit>=4.6.0",
    "pytest>=9.0.3",
]
